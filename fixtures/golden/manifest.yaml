# Public synthetic golden fixture cases.
# These run without any private REPOS or production database.
#
# For operator-specific cases (real SQL tables, production repos),
# copy this file to your overlay:
#   $ORION_OVERLAY_ROOT/fixtures/golden/manifest.yaml
# and add your cases there.

cases:
  - id: manifest_loads
    kind: smoke
    description: Golden manifest and runner import successfully
    repo: RAG-PIPELINE

  # Add SQL cases once you have a local database with data:
  #
  # - id: example_table_query
  #   kind: sql_scalar
  #   description: Example table is queryable
  #   repo: SQL-SCRIPTS
  #   sql: "SELECT COUNT(*) AS cnt FROM your_table"
  #   expect:
  #     min_scalar: 0
