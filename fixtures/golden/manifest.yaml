# Public synthetic golden fixture cases.
# These run without any private REPOS or production database.
#
# Operator-specific cases (real SQL tables, production repos) load from
# $ORION_OVERLAY_ROOT/fixtures/golden/manifest.yaml when present.

cases:
  - id: manifest_loads
    kind: smoke
    description: Golden manifest and runner import successfully
    repo: RAG-PIPELINE

  # Example SQL cases (require local MySQL with data):
  #
  # - id: example_table_query
  #   kind: sql_scalar
  #   description: Example table is queryable
  #   repo: SQL-SCRIPTS
  #   sql: "SELECT COUNT(*) AS cnt FROM your_table"
  #   expect:
  #     min_scalar: 0
