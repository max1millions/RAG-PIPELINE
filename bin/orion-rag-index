#!/usr/bin/env bash
set -euo pipefail
STACK_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
# Quieter ONNX Runtime logs (CPU vendor warnings on some VMs).
export ORT_LOG_LEVEL="${ORT_LOG_LEVEL:-3}"
exec "$STACK_ROOT/.venv/bin/python" "$STACK_ROOT/rag/index.py" "$@"
